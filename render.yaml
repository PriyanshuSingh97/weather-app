services:
  - type: web
    name: weather-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: WEATHER_API_KEY
        value: e83f82cbf0461de3f05e40ccc0335bb2
